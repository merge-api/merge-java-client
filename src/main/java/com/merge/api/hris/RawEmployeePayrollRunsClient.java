/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.merge.api.hris;

import com.merge.api.core.ApiError;
import com.merge.api.core.ClientOptions;
import com.merge.api.core.MergeApiHttpResponse;
import com.merge.api.core.MergeException;
import com.merge.api.core.ObjectMappers;
import com.merge.api.core.QueryStringMapper;
import com.merge.api.core.RequestOptions;
import com.merge.api.core.SyncPagingIterable;
import com.merge.api.hris.types.EmployeePayrollRun;
import com.merge.api.hris.types.EmployeePayrollRunsListRequest;
import com.merge.api.hris.types.EmployeePayrollRunsRetrieveRequest;
import com.merge.api.hris.types.PaginatedEmployeePayrollRunList;
import java.io.IOException;
import java.util.Collections;
import java.util.List;
import java.util.Optional;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawEmployeePayrollRunsClient {
    protected final ClientOptions clientOptions;

    public RawEmployeePayrollRunsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a list of <code>EmployeePayrollRun</code> objects.
     */
    public MergeApiHttpResponse<SyncPagingIterable<EmployeePayrollRun>> list() {
        return list(EmployeePayrollRunsListRequest.builder().build());
    }

    /**
     * Returns a list of <code>EmployeePayrollRun</code> objects.
     */
    public MergeApiHttpResponse<SyncPagingIterable<EmployeePayrollRun>> list(EmployeePayrollRunsListRequest request) {
        return list(request, null);
    }

    /**
     * Returns a list of <code>EmployeePayrollRun</code> objects.
     */
    public MergeApiHttpResponse<SyncPagingIterable<EmployeePayrollRun>> list(
            EmployeePayrollRunsListRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("hris/v1/employee-payroll-runs");
        if (request.getCreatedAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "created_after", request.getCreatedAfter().get().toString(), false);
        }
        if (request.getCreatedBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "created_before", request.getCreatedBefore().get().toString(), false);
        }
        if (request.getCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "cursor", request.getCursor().get(), false);
        }
        if (request.getEmployeeId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "employee_id", request.getEmployeeId().get(), false);
        }
        if (request.getEndedAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "ended_after", request.getEndedAfter().get().toString(), false);
        }
        if (request.getEndedBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "ended_before", request.getEndedBefore().get().toString(), false);
        }
        if (request.getIncludeDeletedData().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "include_deleted_data",
                    request.getIncludeDeletedData().get().toString(),
                    false);
        }
        if (request.getIncludeRemoteData().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "include_remote_data",
                    request.getIncludeRemoteData().get().toString(),
                    false);
        }
        if (request.getIncludeShellData().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "include_shell_data",
                    request.getIncludeShellData().get().toString(),
                    false);
        }
        if (request.getModifiedAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "modified_after", request.getModifiedAfter().get().toString(), false);
        }
        if (request.getModifiedBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "modified_before",
                    request.getModifiedBefore().get().toString(),
                    false);
        }
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_size", request.getPageSize().get().toString(), false);
        }
        if (request.getPayrollRunId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "payroll_run_id", request.getPayrollRunId().get(), false);
        }
        if (request.getRemoteId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "remote_id", request.getRemoteId().get(), false);
        }
        if (request.getStartedAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "started_after", request.getStartedAfter().get().toString(), false);
        }
        if (request.getStartedBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "started_before", request.getStartedBefore().get().toString(), false);
        }
        if (request.getExpand().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "expand", request.getExpand().get().toString(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                PaginatedEmployeePayrollRunList parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                        responseBody.string(), PaginatedEmployeePayrollRunList.class);
                Optional<String> startingAfter = parsedResponse.getNext();
                EmployeePayrollRunsListRequest nextRequest = EmployeePayrollRunsListRequest.builder()
                        .from(request)
                        .cursor(startingAfter)
                        .build();
                List<EmployeePayrollRun> result = parsedResponse.getResults().orElse(Collections.emptyList());
                return new MergeApiHttpResponse<>(
                        new SyncPagingIterable<EmployeePayrollRun>(
                                startingAfter.isPresent(), result, () -> list(nextRequest, requestOptions)
                                        .body()),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new ApiError(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new MergeException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns an <code>EmployeePayrollRun</code> object with the given <code>id</code>.
     */
    public MergeApiHttpResponse<EmployeePayrollRun> retrieve(String id) {
        return retrieve(id, EmployeePayrollRunsRetrieveRequest.builder().build());
    }

    /**
     * Returns an <code>EmployeePayrollRun</code> object with the given <code>id</code>.
     */
    public MergeApiHttpResponse<EmployeePayrollRun> retrieve(String id, EmployeePayrollRunsRetrieveRequest request) {
        return retrieve(id, request, null);
    }

    /**
     * Returns an <code>EmployeePayrollRun</code> object with the given <code>id</code>.
     */
    public MergeApiHttpResponse<EmployeePayrollRun> retrieve(
            String id, EmployeePayrollRunsRetrieveRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("hris/v1/employee-payroll-runs")
                .addPathSegment(id);
        if (request.getIncludeRemoteData().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "include_remote_data",
                    request.getIncludeRemoteData().get().toString(),
                    false);
        }
        if (request.getIncludeShellData().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "include_shell_data",
                    request.getIncludeShellData().get().toString(),
                    false);
        }
        if (request.getExpand().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "expand", request.getExpand().get().toString(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MergeApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), EmployeePayrollRun.class), response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new ApiError(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new MergeException("Network error executing HTTP request", e);
        }
    }
}
