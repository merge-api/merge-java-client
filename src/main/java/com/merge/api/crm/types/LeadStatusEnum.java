/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.merge.api.crm.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class LeadStatusEnum {
    public static final LeadStatusEnum CLOSED = new LeadStatusEnum(Value.CLOSED, "CLOSED");

    public static final LeadStatusEnum QUALIFIED = new LeadStatusEnum(Value.QUALIFIED, "QUALIFIED");

    public static final LeadStatusEnum UNQUALIFIED = new LeadStatusEnum(Value.UNQUALIFIED, "UNQUALIFIED");

    public static final LeadStatusEnum OPEN = new LeadStatusEnum(Value.OPEN, "OPEN");

    private final Value value;

    private final String string;

    LeadStatusEnum(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof LeadStatusEnum && this.string.equals(((LeadStatusEnum) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case CLOSED:
                return visitor.visitClosed();
            case QUALIFIED:
                return visitor.visitQualified();
            case UNQUALIFIED:
                return visitor.visitUnqualified();
            case OPEN:
                return visitor.visitOpen();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static LeadStatusEnum valueOf(String value) {
        switch (value) {
            case "CLOSED":
                return CLOSED;
            case "QUALIFIED":
                return QUALIFIED;
            case "UNQUALIFIED":
                return UNQUALIFIED;
            case "OPEN":
                return OPEN;
            default:
                return new LeadStatusEnum(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        OPEN,

        CLOSED,

        UNQUALIFIED,

        QUALIFIED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitOpen();

        T visitClosed();

        T visitUnqualified();

        T visitQualified();

        T visitUnknown(String unknownType);
    }
}
